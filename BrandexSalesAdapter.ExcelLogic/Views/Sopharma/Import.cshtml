@model BrandexSalesAdapter.ExcelLogic.Models.Sopharma.SopharmaOutputModel
<h1>Sopharma @Model.Date upload was successful!</h1>
@if (Model.Errors.Count > 0)
{
    <h2>The following errors occurred:</h2>
    <ul>

        @foreach (var error in Model.Errors)
        {
            <li>Error: In line @error.Key with value @error.Value</li>
        }

    </ul>
}
@Html.Raw(@Model.Table)